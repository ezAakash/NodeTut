* websockets vs webRTC 


websockets uses persistance TCP connection , while webRTC uses UDP connections.

which means the data we can preferably want to not lost ( not once ) - use Websockets 
while on other hand we can go with - webRTC ( video, audio, 2-d or 3-d metaverse)


# polling concept 
- dede , nahi mila dede .. nhi mili dede 
client ask again and again 


# long polling :
bhai jab bhi move kre dediyo. 


# let's build a echo application 


        browser  ----------------------  server 



Frontend (React)
   runs in browser
   logs → browser console

Backend (Node WebSocket server)
   runs in terminal
   logs → terminal


websocket servers are : sticky and stateful , means all the users in the same room , 
must be on same server. 

        

# difficulty in upgrading n downgrading websocket server 
# single nodejs ws sever maybe load up to 10k connections at a time.
# so, if we are building something like pubg room , this artitecture fails 

# new artitectue we should be adapting is : pub sub - where websockets need to communicate to each other too . 


